import styled from "styled-components"

// just css for the divs in terms of positioning and style
export const ScheduleDiv = styled.div`
    position: fixed;
    display: flex;
    flex-direction: column;
    align-items: center;
`

export const Text = styled.p`
    margin: 5px;
`